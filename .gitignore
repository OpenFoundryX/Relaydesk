.env
.env.local
__pycache__
*.pyc
.venv
node_modules
.pnpm-store
.next
.DS_Store
.pytest_cache
.mypy_cache
.ruff_cache
celerybeat-schedule*
coverage
dist
build

# TypeScript incremental build cache
*.tsbuildinfo

# Agent-created git worktrees. These are linked worktrees, not content:
# committing one would drag a second checkout of the whole tree into the
# repository. `.superpowers/` needs no entry -- it ships its own.
.claude/worktrees/
