/** Small, unobtrusive branding -- the same line the portal's footer prints. */
export function Footer() {
  return (
    <footer className="shrink-0 border-t border-ink-100 px-4 py-2 dark:border-ink-800">
      <p className="text-[11px] text-ink-400 dark:text-ink-500">Powered by Relaydesk</p>
    </footer>
  );
}
